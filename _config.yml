domain: ajhergenroeder.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: "https://ajhergenroeder.github.io"  # the base hostname and protocol for your site, e.g. http://example.com
baseurl: "" # the subpath of your site, e.g. /blog/
baseurl: /docs/      # place folder name if the site is served in a subfolder